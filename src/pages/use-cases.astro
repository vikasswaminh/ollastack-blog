---
import Base from "../layouts/Base.astro";
const APP = "https://login.ollastack.com";
---

<Base
  current="use-cases"
  title="Form backend use cases — contact, agents, webhooks | Ollastack"
  description="Copy-paste curl examples for every form you'd hook to Ollastack: contact, newsletter, job applications, agent submissions, webhooks, and OpenAPI discovery."
>
  <!-- =============================================================
       HERO
       ============================================================= -->
  <section class="hero">
    <div class="container hero-inner">
      <span class="chip mono">
        <span class="dot"></span>
        USE CASES · COPY-PASTE CURL
      </span>
      <h1 class="display headline">
        Every form you'd<br />
        ever <span class="brand-red-text">need a backend for</span>.
      </h1>
      <p class="subhead">
        Real curl. Real payloads. One endpoint serves your marketing contact form,
        your job-applications inbox, your AI agent, and your mobile app — without
        a line of backend code on your side.
      </p>
      <div class="hero-ctas">
        <a class="btn btn-primary btn-lg" href={`${APP}/register`}>
          Start free
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4">
            <path d="M5 12h14M13 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <a class="btn btn-ghost btn-lg" href="/docs">Read the docs</a>
      </div>
      <p class="hero-note mono">
        Three categories below: <a href="#for-websites">websites</a> ·
        <a href="#for-agents">AI agents</a> ·
        <a href="#for-developers">developer infrastructure</a>.
      </p>
    </div>
  </section>

  <!-- =============================================================
       § 1 — For websites (classic form-to-email)
       ============================================================= -->
  <section class="cases" id="for-websites">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">/ for websites</span>
        <h2 class="display section-title">
          Drop-in backend for any web form.
        </h2>
        <p class="section-sub">
          The Formspree pattern, done right. One POST, email in your inbox,
          spam pipeline already running. Zero server code on your side.
        </p>
      </div>

      <!-- 1. Marketing contact form -->
      <article class="usecase">
        <div class="usecase-head">
          <span class="usecase-tag mono">contact form</span>
          <h3 class="usecase-title">Marketing site contact form</h3>
          <p class="usecase-body">
            The classic. A lead fills the form, the sales team gets the email, and
            <code class="inline-code">_replyto</code> rewrites the email's Reply-To header
            — so when sales hits <strong>Reply</strong>, the message goes to the lead, not to us.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">contact</span> \
    -H <span class="str">"Content-Type: application/json"</span> \
    -d <span class="str">'&#123;
      "name": "Ada Lovelace",
      "email": "ada@example.com",
      "message": "Interested in a demo for our 12-person team.",
      "_replyto": "ada@example.com"
    &#125;'</span>

<span class="ok">&#123; "success": true, "id": "sub_8f2a91c…" &#125;</span>
<span class="muted"># Email lands in sales@yourco.com.
# Reply-To: ada@example.com → hit reply, talk to the lead.</span></pre>
      </article>

      <!-- 2. Newsletter with autoresponder -->
      <article class="usecase">
        <div class="usecase-head">
          <span class="usecase-tag mono">newsletter</span>
          <h3 class="usecase-title">Newsletter signup with instant autoresponder</h3>
          <p class="usecase-body">
            <code class="inline-code">_autoresponse</code> sends the subscriber a confirmation
            email the moment they submit. No transactional-email provider needed for this one
            common case.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">newsletter</span> \
    -H <span class="str">"Content-Type: application/json"</span> \
    -d <span class="str">'&#123;
      "email": "reader@example.com",
      "_autoresponse": "Thanks for subscribing — your first issue ships Friday.",
      "_subject": "New subscriber: reader@example.com"
    &#125;'</span>

<span class="ok">&#123; "success": true, "id": "sub_a14f…" &#125;</span>
<span class="muted"># You get the lead. The subscriber gets an instant confirm reply.</span></pre>
      </article>

      <!-- 3. Job application with file upload -->
      <article class="usecase">
        <div class="usecase-head">
          <span class="usecase-tag mono">careers</span>
          <h3 class="usecase-title">Job application with resume upload</h3>
          <p class="usecase-body">
            Multipart parser handles PDFs, docs, screenshots. The resume gets attached to the
            recruiter's notification email. The <code class="inline-code">[Possible spam]</code>
            ML quarantine keeps the inbox clean without ever silently dropping a real applicant.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">careers-backend</span> \
    -F <span class="str">"name=Grace Hopper"</span> \
    -F <span class="str">"email=grace@example.com"</span> \
    -F <span class="str">"role=Senior backend engineer"</span> \
    -F <span class="str">"years_experience=8"</span> \
    -F <span class="str">"cover_letter=I've shipped distributed systems at..."</span> \
    -F <span class="str">"resume=@./resume.pdf"</span>

<span class="ok">&#123; "success": true, "id": "sub_c01dd…" &#125;</span>
<span class="muted"># Notification email to recruiter@yourco.com has resume.pdf attached.</span></pre>
      </article>

      <!-- 4. Bug report with subject routing -->
      <article class="usecase">
        <div class="usecase-head">
          <span class="usecase-tag mono">support intake</span>
          <h3 class="usecase-title">Bug report widget with subject routing</h3>
          <p class="usecase-body">
            <code class="inline-code">_subject</code> overrides the default email subject so your
            support inbox's filter rules route by severity, product, or environment without you
            running a triage script.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">bug-report</span> \
    -H <span class="str">"Content-Type: application/json"</span> \
    -d <span class="str">'&#123;
      "title": "Dashboard crashes on Safari 17",
      "severity": "high",
      "product_area": "billing",
      "steps_to_reproduce": "1. Open /orgs/&#123;id&#125;  2. Click Upgrade  3. White screen",
      "browser": "Safari 17.4 / macOS 14.5",
      "_subject": "[BUG][HIGH][billing] Dashboard crashes on Safari 17",
      "_replyto": "reporter@example.com"
    &#125;'</span>

<span class="ok">&#123; "success": true, "id": "sub_b9c12…" &#125;</span>
<span class="muted"># Gmail filter on "[BUG][HIGH]" pages on-call. The rest go to triage.</span></pre>
      </article>
    </div>
  </section>

  <!-- =============================================================
       § 2 — For AI agents (the differentiator, biggest section)
       ============================================================= -->
  <section class="cases cases-agents" id="for-agents">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">/ for AI agents 🤖</span>
        <h2 class="display section-title">
          Forms your agent can use as a first-class citizen.
        </h2>
        <p class="section-sub">
          Bearer-authed POSTs skip captcha, honeypot, and the per-IP rate limit
          — the token IS the trust boundary. The OpenAPI spec is CORS-open so
          the agent discovers the surface itself.
        </p>
      </div>

      <!-- 5. Agent books a demo on user's behalf -->
      <article class="usecase usecase-accent">
        <div class="usecase-head">
          <span class="usecase-tag mono">agent · bearer</span>
          <h3 class="usecase-title">Agent books a demo on a user's behalf</h3>
          <p class="usecase-body">
            Your assistant (Claude, ChatGPT, a custom one) fills out the prospect's demo form
            for them — the lead lands in your sales inbox stamped with the agent's label,
            attributed in the audit log, no captcha to break.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">demo-request</span> \
    -H <span class="str">"Authorization: Bearer fmd_a8c91b2e7d…"</span> \
    -H <span class="str">"Content-Type: application/json"</span> \
    -d <span class="str">'&#123;
      "name": "Sarah Chen",
      "company": "Acme Corp",
      "email": "sarah@acme.com",
      "team_size": 12,
      "use_case": "Lead capture for marketing site + agent integration",
      "monthly_volume": 5000,
      "_replyto": "sarah@acme.com",
      "_source": "submitted via Claude.ai on user behalf"
    &#125;'</span>

<span class="ok">&#123;
  "success": true,
  "id": "sub_d31be…",
  "submitted_via": "agent:tok_demo_intake_claude"
&#125;</span>
<span class="muted"># Inbox row shows the 🤖 stamp + the token label.
# Skipped: per-IP rate limit, honeypot, CAPTCHA. The token is trust.</span></pre>
      </article>

      <!-- 6. Bulk import -->
      <article class="usecase usecase-accent">
        <div class="usecase-head">
          <span class="usecase-tag mono">agent · bulk</span>
          <h3 class="usecase-title">Bulk import 1,000 leads from a scrape, CSV, or Sheet</h3>
          <p class="usecase-body">
            Idempotency keys make the loop safely retryable — re-running the same script doesn't
            double-post. Per-IP rate limit doesn't apply to Bearer-authed traffic, so a
            background job can drain a queue without throttling.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">cat</span> leads.jsonl <span class="pun">|</span> <span class="cmd">while</span> read line; <span class="cmd">do</span>
    <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">crm-intake</span> \
      -H <span class="str">"Authorization: Bearer fmd_a8c91b2e7d…"</span> \
      -H <span class="str">"Content-Type: application/json"</span> \
      -H <span class="str">"Idempotency-Key: $(echo -n "$line" | sha256sum | cut -c1-32)"</span> \
      -d <span class="str">"$line"</span>
  <span class="cmd">done</span>

<span class="ok">&#123; "success": true, "id": "sub_…" &#125;   ×1000</span>
<span class="muted"># Re-run with the same input and you get the same IDs — no dupes.
# Per-IP rate limit doesn't apply to Bearer tokens.</span></pre>
      </article>

      <!-- 7. OpenAPI discovery -->
      <article class="usecase usecase-accent">
        <div class="usecase-head">
          <span class="usecase-tag mono">agent · discovery</span>
          <h3 class="usecase-title">Agent discovers the API surface itself</h3>
          <p class="usecase-body">
            The whole API is described at <code class="inline-code">/api/openapi.json</code> —
            CORS-open, no auth required to read. Drop it into an agent's tool-use loop and it
            learns the endpoints, parameter types, and response shapes without a docs site.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -s https://login.ollastack.com/api/openapi.json <span class="pun">|</span> <span class="cmd">jq</span> <span class="str">'.paths | keys'</span>

<span class="ok">[
  "/api/forms",
  "/api/forms/&#123;formId&#125;",
  "/api/forms/&#123;formId&#125;/builder",
  "/api/forms/&#123;formId&#125;/submissions",
  "/api/forms/&#123;formId&#125;/failures",
  "/api/submit/&#123;endpointSlug&#125;",
  "/api/submissions/&#123;id&#125;",
  "/api/tokens",
  …
]</span>
<span class="muted"># Agent reads, mints a scoped token, picks an endpoint, submits.
# No SDK install, no docs to scrape. Just JSON.</span></pre>
      </article>

      <!-- 8. Voice → form -->
      <article class="usecase usecase-accent">
        <div class="usecase-head">
          <span class="usecase-tag mono">agent · voice</span>
          <h3 class="usecase-title">Voice agent → schema-validated form</h3>
          <p class="usecase-body">
            Your voice agent (Twilio, Vapi, Retell) transcribes a call, extracts the structured
            fields, and submits. The form's strict 13-type schema catches malformed extractions
            before they pollute the inbox — bad voice agents fail loudly, not silently.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">clinic-intake</span> \
    -H <span class="str">"Authorization: Bearer fmd_voice_agent_…"</span> \
    -H <span class="str">"Content-Type: application/json"</span> \
    -d <span class="str">'&#123;
      "patient_name": "Anna Smith",
      "phone": "+1 415 555 0117",
      "reason_for_visit": "Follow-up — knee injection three weeks ago",
      "preferred_date": "2026-06-15",
      "preferred_time": "afternoon",
      "insurance_provider": "Blue Shield",
      "_source": "via Twilio voice agent",
      "_replyto": "anna.smith@example.com"
    &#125;'</span>

<span class="ok">&#123; "success": true, "id": "sub_v8c1e…" &#125;</span>
<span class="muted"># Schema rejects bad date formats with 400 + a field-level error
# the agent can react to ("please confirm the date").</span></pre>
      </article>

      <!-- 9. Workflow trigger -->
      <article class="usecase usecase-accent">
        <div class="usecase-head">
          <span class="usecase-tag mono">agent · workflow</span>
          <h3 class="usecase-title">Agent logs a workflow event from Slack / GitHub / a file watcher</h3>
          <p class="usecase-body">
            Use a form as a write-side event log. The agent submits, the form's signed webhook
            fires to your downstream system, and the auto-retry (5× with backoff) means your
            pipeline doesn't lose the event if its receiver is briefly down.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="hl">build-events</span> \
    -H <span class="str">"Authorization: Bearer fmd_ci_agent_…"</span> \
    -H <span class="str">"Content-Type: application/json"</span> \
    -d <span class="str">'&#123;
      "kind": "deploy.completed",
      "service": "api-gateway",
      "git_sha": "8f3a91c",
      "env": "production",
      "duration_ms": 47120,
      "triggered_by": "release-bot"
    &#125;'</span>

<span class="ok">&#123; "success": true, "id": "sub_evt_…" &#125;</span>
<span class="muted"># Webhook fires to your audit-log service.
# Auto-retried at 1m, 5m, 30m, 2h, 10h if it 5xxs.</span></pre>
      </article>
    </div>
  </section>

  <!-- =============================================================
       § 3 — For developers (infra under the form layer)
       ============================================================= -->
  <section class="cases" id="for-developers">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">/ for developers</span>
        <h2 class="display section-title">
          Real infra under the form layer.
        </h2>
        <p class="section-sub">
          Auto-retry'd webhooks, multipart parsing, idempotency, signed deliveries,
          per-tenant SMTP. Your prototype graduates to production without
          re-platforming.
        </p>
      </div>

      <!-- 10. Static site — no backend -->
      <article class="usecase">
        <div class="usecase-head">
          <span class="usecase-tag mono">static site</span>
          <h3 class="usecase-title">A real backend for your static site</h3>
          <p class="usecase-body">
            Drop this on Vercel, Cloudflare Pages, Netlify, S3, GitHub Pages. The form
            URL handles CORS, parses urlencoded and JSON, runs the spam pipeline, and emails
            you. Zero server code on your side.
          </p>
        </div>
        <pre class="code-pane"><span class="pun">&lt;form action=</span><span class="str">"https://login.ollastack.com/api/submit/contact"</span> method=<span class="str">"POST"</span><span class="pun">&gt;</span>
  <span class="pun">&lt;input name=</span><span class="str">"name"</span> required<span class="pun">/&gt;</span>
  <span class="pun">&lt;input name=</span><span class="str">"email"</span> type=<span class="str">"email"</span> required<span class="pun">/&gt;</span>
  <span class="pun">&lt;textarea name=</span><span class="str">"message"</span> required<span class="pun">&gt;&lt;/textarea&gt;</span>
  <span class="pun">&lt;input type=</span><span class="str">"hidden"</span> name=<span class="str">"_subject"</span> value=<span class="str">"New contact — yourco.com"</span><span class="pun">/&gt;</span>
  <span class="pun">&lt;button&gt;</span>Send<span class="pun">&lt;/button&gt;</span>
<span class="pun">&lt;/form&gt;</span>
<span class="muted">&lt;!-- That's it. No PHP, no Vercel function, no SMTP setup. --&gt;</span></pre>
      </article>

      <!-- 11. Webhook receiver -->
      <article class="usecase">
        <div class="usecase-head">
          <span class="usecase-tag mono">webhooks</span>
          <h3 class="usecase-title">Verify and consume a signed webhook</h3>
          <p class="usecase-body">
            Every webhook delivery carries an HMAC-SHA256 signature you verify with
            <code class="inline-code">timingSafeEqual</code>. If your endpoint returns 5xx,
            we retry 5× with exponential backoff (1m → 5m → 30m → 2h → 10h) — your pipeline
            doesn't lose events on flaky deploys.
          </p>
        </div>
        <pre class="code-pane"><span class="key">import</span> &#123; createHmac, timingSafeEqual &#125; <span class="key">from</span> <span class="str">"node:crypto"</span>;

<span class="key">export function</span> <span class="fn">verifyOllastackWebhook</span>(headers, rawBody) &#123;
  <span class="key">const</span> sig = headers[<span class="str">"x-ollastack-signature"</span>];
  <span class="key">const</span> expected = <span class="fn">createHmac</span>(<span class="str">"sha256"</span>, process.env.OLLASTACK_WEBHOOK_SECRET)
    .<span class="fn">update</span>(rawBody)
    .<span class="fn">digest</span>(<span class="str">"hex"</span>);
  <span class="key">return</span> sig.length === expected.length
    <span class="pun">&amp;&amp;</span> <span class="fn">timingSafeEqual</span>(Buffer.<span class="fn">from</span>(sig), Buffer.<span class="fn">from</span>(expected));
&#125;

<span class="key">export async function</span> <span class="fn">POST</span>(req) &#123;
  <span class="key">const</span> raw = <span class="key">await</span> req.<span class="fn">text</span>();
  <span class="key">if</span> (!<span class="fn">verifyOllastackWebhook</span>(req.headers, raw)) <span class="key">return</span> <span class="key">new</span> <span class="fn">Response</span>(<span class="str">"bad sig"</span>, &#123; status: 401 &#125;);
  <span class="key">const</span> payload = JSON.<span class="fn">parse</span>(raw);
  <span class="key">await</span> <span class="fn">enqueueForProcessing</span>(payload);
  <span class="key">return</span> <span class="key">new</span> <span class="fn">Response</span>(<span class="str">"ok"</span>);
&#125;</pre>
      </article>

      <!-- 12. List + export submissions via API -->
      <article class="usecase">
        <div class="usecase-head">
          <span class="usecase-tag mono">read API</span>
          <h3 class="usecase-title">List, filter, and export submissions programmatically</h3>
          <p class="usecase-body">
            The same Bearer token your agent uses to submit can read submissions back — scoped
            to <code class="inline-code">submissions:read</code>. Pipe to your warehouse, your
            CRM, your nightly job.
          </p>
        </div>
        <pre class="code-pane"><span class="prompt">$</span> <span class="cmd">curl</span> -s <span class="str">"https://login.ollastack.com/api/forms/frm_8f2a/submissions?since=2026-06-01&amp;limit=200"</span> \
    -H <span class="str">"Authorization: Bearer fmd_read_only_…"</span> \
  <span class="pun">|</span> <span class="cmd">jq</span> <span class="str">'.data[] | &#123;id, email: .data.email, created_at&#125;'</span>

<span class="ok">&#123; "id": "sub_a14f…", "email": "ada@example.com", "created_at": "2026-06-02T09:14:21Z" &#125;
&#123; "id": "sub_b9c1…", "email": "grace@example.com", "created_at": "2026-06-02T11:02:48Z" &#125;
…</span>
<span class="muted"># Token scope enforced server-side. submissions:write token can't read.</span></pre>
      </article>
    </div>
  </section>

  <!-- =============================================================
       Closing CTA
       ============================================================= -->
  <section class="closing">
    <div class="container">
      <div class="closing-band">
        <h2 class="closing-title">Pick one. Ship it in five minutes.</h2>
        <p class="closing-sub">
          Free tier: 50 submissions / month, every feature. No credit card.
          <a href="/pricing">See pricing</a> for higher volume.
        </p>
        <div class="closing-ctas">
          <a class="btn btn-primary btn-lg" href={`${APP}/register`}>
            Start free
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4">
              <path d="M5 12h14M13 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
          <a class="btn btn-ghost btn-lg" href="/docs">Read the docs</a>
        </div>
      </div>
    </div>
  </section>

</Base>

<style>
  /* ---- Hero (tighter than home, since cards dominate the page) ---- */
  .hero {
    padding: 64px 0 48px;
    border-bottom: 1px solid var(--line);
  }
  .hero-inner { display: flex; flex-direction: column; align-items: flex-start; }
  .chip .dot {
    width: 7px;
    height: 7px;
    border-radius: 999px;
    background: var(--accent);
    box-shadow: 0 0 0 3px rgba(0, 102, 255, 0.18);
  }
  .headline {
    font-size: 64px;
    margin: 20px 0 18px;
    letter-spacing: -0.035em;
    line-height: 0.98;
    max-width: 16ch;
    color: var(--ink);
  }
  .headline .brand-red-text { color: var(--brand-red); }
  .subhead {
    font-size: 17px;
    color: var(--muted);
    line-height: 1.6;
    max-width: 620px;
    margin: 0 0 24px;
  }
  .hero-ctas { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
  .hero-note {
    margin-top: 20px;
    color: var(--muted-2);
    font-size: 12.5px;
  }
  .hero-note a {
    color: var(--ink);
    border-bottom: 1px dashed var(--muted-3);
  }
  .hero-note a:hover { border-bottom-color: var(--ink); }
  .inline-code {
    background: var(--bg-sunken);
    border: 1px solid var(--line);
    padding: 1px 6px;
    border-radius: var(--r-1);
    font-family: var(--font-mono);
    font-size: 0.9em;
    color: var(--ink-2);
  }

  /* ---- Section heads ---- */
  .section-head {
    margin-bottom: 36px;
    max-width: 720px;
  }
  .section-head .eyebrow { display: inline-block; margin-bottom: 12px; }
  .section-title {
    font-size: 36px;
    margin: 0 0 14px;
    letter-spacing: -0.025em;
  }
  .section-sub {
    font-size: 15.5px;
    color: var(--muted);
    line-height: 1.6;
    margin: 0;
    max-width: 600px;
  }

  /* ---- Case sections ---- */
  .cases { padding: 72px 0; border-top: 1px solid var(--line); }
  .cases-agents {
    background: var(--bg-elev);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  /* ---- Usecase card ---- */
  .usecase {
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: var(--r-4);
    overflow: hidden;
    margin-bottom: 18px;
    transition: border-color 0.15s ease, box-shadow 0.15s ease;
  }
  .usecase:last-child { margin-bottom: 0; }
  .usecase:hover {
    border-color: var(--line-2);
    box-shadow: 0 1px 0 rgba(10, 10, 10, 0.03), 0 18px 36px -16px rgba(10, 10, 10, 0.08);
  }
  /* Agents get a slightly stronger frame to mark them as the differentiator */
  .usecase-accent {
    border-color: var(--ink);
    box-shadow: 0 0 0 1px rgba(10, 10, 10, 0.04);
  }
  .usecase-accent:hover {
    box-shadow: 0 0 0 1px rgba(10, 10, 10, 0.04), 0 24px 48px -16px rgba(10, 10, 10, 0.16);
  }

  .usecase-head {
    padding: 22px 26px 18px;
  }
  .usecase-tag {
    display: inline-block;
    font-size: 10.5px;
    color: var(--muted-2);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 3px 8px;
    border: 1px solid var(--line);
    border-radius: 999px;
    background: var(--bg-elev);
    margin-bottom: 12px;
  }
  .usecase-accent .usecase-tag {
    color: var(--accent-2);
    background: var(--accent-soft);
    border-color: #cfdcff;
  }
  .usecase-title {
    font-size: 21px;
    font-weight: 600;
    letter-spacing: -0.015em;
    margin: 0 0 8px;
    color: var(--ink);
    line-height: 1.25;
  }
  .usecase-body {
    font-size: 14.5px;
    color: var(--muted);
    line-height: 1.6;
    margin: 0;
    max-width: 72ch;
  }
  .usecase-body strong { color: var(--ink-2); font-weight: 600; }

  /* ---- Code pane ---- */
  .code-pane {
    background: var(--code-bg);
    color: var(--code-fg);
    font-family: var(--font-mono);
    font-size: 13px;
    line-height: 1.7;
    margin: 0;
    padding: 22px 26px;
    border-top: 1px solid #1f1f22;
    white-space: pre-wrap;
    word-break: break-word;
    overflow-x: auto;
  }
  .code-pane .prompt { color: var(--code-prompt); margin-right: 8px; }
  .code-pane .cmd    { color: var(--code-cmd); font-weight: 600; }
  .code-pane .str    { color: var(--code-str); }
  .code-pane .key    { color: var(--code-key); }
  .code-pane .fn     { color: var(--code-fn); }
  .code-pane .pun    { color: var(--code-pun); }
  .code-pane .hl     { color: var(--accent); }
  .code-pane .ok     { color: var(--code-ok); display: inline-block; margin-top: 8px; }
  .code-pane .muted  { color: var(--code-muted, #6b7280); display: inline-block; margin-top: 8px; }

  /* ---- Closing CTA ---- */
  .closing {
    padding: 88px 0 104px;
    background: var(--bg-elev);
    border-top: 1px solid var(--line);
  }
  .closing-band { max-width: 720px; margin: 0 auto; text-align: center; }
  .closing-title {
    font-size: 36px;
    letter-spacing: -0.025em;
    line-height: 1.1;
    margin: 0 0 14px;
    color: var(--ink);
  }
  .closing-sub {
    font-size: 16px;
    color: var(--muted);
    line-height: 1.6;
    margin: 0 0 24px;
    max-width: 540px;
    margin-left: auto;
    margin-right: auto;
  }
  .closing-sub a {
    color: var(--ink);
    border-bottom: 1px dashed var(--muted-3);
  }
  .closing-ctas {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
    justify-content: center;
  }

  /* ---- Responsive ---- */
  @media (max-width: 960px) {
    .headline { font-size: 48px; }
    .section-title { font-size: 28px; }
    .usecase-title { font-size: 19px; }
    .closing-title { font-size: 28px; }
  }
  @media (max-width: 600px) {
    .headline { font-size: 36px; }
    .usecase-head { padding: 18px 18px 14px; }
    .code-pane { padding: 18px 18px; font-size: 12px; }
    .section-title { font-size: 24px; }
  }
</style>
